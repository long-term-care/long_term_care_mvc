@model IEnumerable<long_term_care.Models.MemSign>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MemTelTime1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MemTelTime2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MemRecord)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MemS)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MemTelTime1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MemTelTime2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MemRecord)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MemS.MemSid)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MemSignQaid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MemSignQaid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MemSignQaid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
